This directory contains the compose file that starts the

fuseki, prez and nginx services

it also has:

- the nginx configuration that describes how to serve the prez-ui static files.
- the fuseki assembler file that describes the dataset configuration for fuseki.
